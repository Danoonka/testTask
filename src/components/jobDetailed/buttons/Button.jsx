import React from 'react';
import "./button.css"

const Button = () => {
    return (
        <div>
            <button className="button_apply">APPLY NOW</button>
        </div>
    );
};

export default Button;